---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: hostedclustertemplates.hcp.gcp.hypershift.openshift.com
spec:
  group: hcp.gcp.hypershift.openshift.com
  names:
    kind: HostedClusterTemplate
    listKind: HostedClusterTemplateList
    plural: hostedclustertemplates
    singular: hostedclustertemplate
  scope: Cluster
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          HostedClusterTemplate is a cluster-scoped template for creating HostedCluster CRs.
          It contains a versioned Go template that is rendered with per-cluster values.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of HostedClusterTemplate
            properties:
              template:
                description: |-
                  template is a raw YAML Go template for a complete HostedCluster CR spec.
                  Rendered with values from ManagedHostedCluster at reconcile time.
                  Supports Go text/template syntax (e.g. {{ .ClusterID }}).
                type: string
              version:
                description: |-
                  version is the OCP version this template targets (e.g. "4.16").
                  The hostedcluster controller selects the template whose version
                  matches the VersionStream's target version.
                type: string
            required:
            - template
            - version
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
